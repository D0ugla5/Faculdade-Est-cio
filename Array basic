var vasco = []

            //adds
vasco.push('Coutinho','Souza', 'Vegetti','Léo Jardim')
alunos[alunos.length] = 'Maria';
          //ads
            //REMOVE
Array.splice(posição // que será inserido ,0 // se algo vai ser excluido ,novo_elemento,novo_elemento,...)

//deleta o último
vasco.pop()
//deleta o primeiro
vasco.shift()

vetor[vetor.length-1]  //acessa o último valor do array

console.log(vasco)
